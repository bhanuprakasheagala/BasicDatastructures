#include<bits/stdc++.h>

using namespace std;

struct Node{
  int data;
  struct Node *next;
}
struct Node *head == NULL;


int main(){
  insert(head, 1);
  insert(head, 2);
  insert(head, 3);
  insert(head, 4);
  insert(head, 5);
  
  return 0;
}
